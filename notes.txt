ISSUEs
- I count not get the custom errors to work in the throw New Errors - Figured that out when testing
- Used AI to leave nice notes above functions to help explaining inside validation.ts, 
- The reason we check why we dont have commas in the reference is because we are using a csv that is broken using commas
- AI was very unhelpful when it came to checking if all my error handling made sense, would always want to make changed when not asked too


- Vite project vs running a script - npm run dev starts a web server, need node src/index.ts / npm start
- __dirname is not defined in ES modules (package.json "type": "module"), had to use import.meta.dirname
- Path joined from src/ without ".." so it looked for src/files instead of files/
- TypeScript wanted .js on imports, Node running .ts files needs .ts or it looks for createBillingFile.js which does not exist
- JSON is header/body/trailer with "references", the app uses BillingRun.collections and "reference", so index.ts has to map the file into a run
- Same-run error did not throw because sameRun was undefined - data/sent.json was empty. recordSent only runs after SFTP succeeds, and rename failed because the remote file already existed
- runId is not created in code, it is already in files/billing-collections.json header and does not change by itself the next day
- SFTP rename does not overwrite. Second send of the same runId.csv on the server fails even if sent.json was not written
- SFTP error wrapper with step/connect/put/rename mapping was too much for 3 hours, stripped it back to one catch
